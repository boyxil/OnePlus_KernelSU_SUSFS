name: 'Save Cache (stub)'
description: 'No-op stub to prevent cache upload failures'
inputs:
  cache_path:
    required: true
  cache_key:
    required: true
  cache_bucket:
    required: false
  github_token:
    required: true
runs:
  using: 'composite'
  steps:
    - name: Skip cache save
      shell: bash
      run: echo "Cache save skipped (stub action)"
